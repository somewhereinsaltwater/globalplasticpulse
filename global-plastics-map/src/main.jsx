import React from 'react';
import ReactDOM from 'react-dom/client';
import GlobalPlasticsMap from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GlobalPlasticsMap />
  </React.StrictMode>
);